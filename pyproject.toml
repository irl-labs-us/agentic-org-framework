[project]
name = "agentic-org-framework"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["scripts/tests"]
addopts = "-q"
